export(expectile.laws,
       expectile.restricted,
       expectile.bundle,
       expectile.boost,
       expectile.sheets,
       bundle.density,
       base,
       quant.boost,
       pkoenker,
       qkoenker,
       dkoenker,
       ekoenker,
       rkoenker,
       ebeta,
       enorm,
       eunif,
       et,
       egamma,
       echisq,
       eexp,
       elnorm)

S3method(plot, expectreg)
S3method(plot, bundledensity)
